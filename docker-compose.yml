services:
  rustqueue:
    build: .
    ports:
      - "6790:6790"
      - "6789:6789"
    volumes:
      - rustqueue-data:/data
      - ./deploy/rustqueue.toml:/etc/rustqueue/rustqueue.toml:ro
    environment:
      - RUST_LOG=info
    restart: unless-stopped

volumes:
  rustqueue-data:
